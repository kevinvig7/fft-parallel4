
git remote remove origin 
%Para ver cambios en al conexion 
git remote -v
%
%
git remote add origin https://github.com/dani6rg/Procesos_EStocasticos.git
%Te desvincula del enlace al repositorio externo

%Para subir la rama en al que estamos
git push origin master

%Para ver los cambios en Commits
git log

%Par ver si las Archivos estan actualizados
git status

%para adherir archivos 
git add -A

%commits
git commit -m "escribe_tu_mensaje"


%%
%Ramas te dice la rama en la que estas
git branch 
%elimino rama
git brach -D nombre_de_rama

%Nueva rama
git branch nombre_rama_nueva
%moverse a la rama creada
git checkout nombre_rama_nueva

%Para subir de otra rama Distinta a Master
git push origin Rama_creada


%%Vincular la rama MASTER con otra rama MERGE
git checkout master
git merge nombre_rama_absorber 
%Luego si quiero subir esos cambios realizo:
git push origin master


%Agregar archivos independientes
git add Archivo.extension 
%Agrega Todos
git add -A


%%%Par ir a commit Pasados:
%obtener el codigo Sha del commit con: git log
git checkout codigo_sha
%%%Para moverse al ultimo Commit
git checkout master

%%Resets 3 tipos
git reset --soft	
git reset --mixed
git reset --hard
%Ejemplo, eliminara el commit cuando lo quiera ver con git log, pero no toca el codigo corresondiente a ese commit
git resert --soft Sha_del_commit


%Genera un .txt con los commits
git log > commits.txt

%ayuda
git help
git help status

%%%FUSIONES, creacion de un nuevo commit, juntando la rama Master y la Rama_Creada

%Crear una rama y moverme a ella
git checkout -b Nueva_Rama_Creada

%Ramas: |master| - |origin/master| - |master|
%bajo los cambios del repositorio remoto de master a |origin master|
git fetch origin
%ahora para combinar el commit de otra persona en el repositorio local FUSIONO la rama que he bajado(repo externo) con la rama local(repo interno)
git merge origin/master
%Esto me llevara a crear un nuevo commit donde estan los combios combinados
%Y luego vuelvo a subir todo con
git push origin master


%Lo siguiente me muestra a cual repositorio me encuentro conectado
git remote -v
%elimino la conexion
git remote remove origin

=============================
➜  Escritorio ssh-keygen -t rsa -C "dani6rg@gmail.com" -b 4096
Generara un ssh
➜  Escritorio cat /home/dani6rg/.ssh/id_rsa.pub 
=============================
➜  Escritorio git clone git@gitlab.fulgor:apola/cursoVLSI.git 
%creo archivo
➜  cursoVLSI git:(master) touch test.txt
➜  cursoVLSI git:(master) ✗ git commit -a -m 'archivo test'

➜  cursoVLSI git:(master) ✗ git add .                      
➜  cursoVLSI git:(master) ✗ git status
➜  cursoVLSI git:(master) git log
➜  cursoVLSI git:(master) git push origin master

============================
listar las ramas 
git branch
Creamos una rama
git branch testing
Borramos una rama
git branch -d testing
git checkout
Si queremos fusionar una rama con la que actualmente estamos trabajando
git merge devel	
git merge

mostrará todos los repositorios remotos
git remote -v
Añadir un repositorio
git remote add octopress git://github.com/imathis/octopress.git
Borrar un repositorio:
git remote rm octopress

git fetch
Despues de un git fetch es conveniente hacer un diff, comparando las ramas de trabajo local con la del repositorio remoto.
git merge









